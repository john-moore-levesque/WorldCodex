# Codex — local dev / deploy configuration
# Copy this file to .env and fill in your values. Never commit .env.

# API Gateway URL (no trailing slash)
VITE_API_URL=https://YOUR_API_ID.execute-api.us-east-1.amazonaws.com/Prod

# AWS region where your resources live
VITE_AWS_REGION=us-east-1

# Cognito Identity Pool (for SigV4 credential vending)
VITE_COGNITO_IDENTITY_POOL_ID=us-east-1:xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Cognito User Pool (for Google OAuth token exchange)
VITE_COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
VITE_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# Cognito Hosted UI domain (no https://, no trailing slash)
VITE_COGNITO_DOMAIN=your-app.auth.us-east-1.amazoncognito.com

# Label for the timeline time axis (singular). Defaults to "Year" if unset.
# Worlds with non-calendar time (e.g. a generation ship) can set this to
# "Mission Day", "Cycle", "Tick", etc. Used in form labels and axis legends.
VITE_TIME_UNIT=Year

# ── World-specific taxonomy (optional) ──
# Override the dropdown choices for each module. Comma-separated; falls back to
# genre-neutral defaults if unset. Use "id:Label" form to provide display
# labels; bare "id" uses a title-cased version of the id.
#
# EVENT_CATEGORIES and TECH_SUBCATEGORIES drive colored chips: each id maps to
# CSS vars --cat-<id> / --cat-<id>-soft (and --tech-<id> / --tech-<id>-soft).
# Add matching color vars in your theme block in codex.jsx for any custom id,
# or it falls back to a neutral palette.
#
# VITE_EVENT_CATEGORIES=political:Political,technology:Technology,military:Military,cultural:Cultural,other:Other
# VITE_FACTION_TYPES=government,military,religious,organization,criminal,other
# VITE_TECH_CATEGORIES=infrastructure,weapons,energy,materials,communications,transport,other
# VITE_TECH_SUBCATEGORIES=transport:Transport,communications:Communications,energy:Energy,weapons:Weapons,materials:Materials
# VITE_LOCATION_TYPES=region,settlement,structure,landmark,vessel,other
# VITE_CONNECTION_TYPES=route,border,passage,other

# ── Deploy targets (used by deploy.sh, not baked into the bundle) ──
DEPLOY_BUCKET=your-s3-bucket-name
# DEPLOY_CLOUDFRONT_ID=EXXXXXXXXXXXX   # optional — omit to skip invalidation
